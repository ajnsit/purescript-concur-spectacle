{
  "name": "concur-spectacle",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "build": "spago build",
    "clean": "rimraf output .cache .psci_modules .pulp-cache .spago dist prod",
    "test": "node scripts/test.js --env=jsdom"
  },
  "devDependencies": {
    "rimraf": "^2.7.1"
  },
  "homepage": "http://ajnsit.github.io/purescript-concur-spectacle"
}
